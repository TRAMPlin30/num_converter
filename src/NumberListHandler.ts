
export class NumberListHandler {


    static handlerOneDigit(indexList: number[], units: string[]): string {

        var index: number = indexList[0];
        var word: string = units[index]; 
            return word;
    }



    static handlerTwoDigit(indexList: number[], units: string[]) {
        // handle two digits numbers
    }


}